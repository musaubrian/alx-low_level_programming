#Dynamic Libraries

This projects takes a deeper dive into libraries - dynamic libraries that is.

**Dynamic libraries** also known as **shared libraries** are collections of functions compiled ad stored in an executable file for it to be linked to other programmes at run-time.
